<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weatherdetector" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherDetector" >
        <activity
            android:name=".ReadingActivity"
            android:exported="true"
            android:label="@string/atmospheric_reading_title" />
        <activity
            android:name=".AirPressureReader"
            android:exported="true"
            android:label="@string/air_press_str" />
        <activity
            android:name=".HumidityReader"
            android:exported="true"
            android:label="@string/hum_read_str" />
        <activity
            android:name=".TemperatureReader"
            android:exported="true"
            android:label="@string/temp_str" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>